# Trip Hub Product Assignments

Store product-side task dispatch to downstream functions here.

Typical use:

- a product review turns into design follow-up
- a product freeze turns into architecture alignment
- a release requires a product acceptance checklist

Rule:

- This folder holds workflow dispatch artifacts, not live product truth.
- Live product truth still belongs under `docs/initiatives/trip-hub/product/`.
